# Emitted by the release assembler in the private development tree. Edits here are overwritten by
# the next release — change the emitter, not this file.

# This tree is assembled with LF endings and compared byte-for-byte against a fresh assembly on the
# private side. A checkout that rewrote line endings would read as permanent drift.
* text=auto eol=lf
